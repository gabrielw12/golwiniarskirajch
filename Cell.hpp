#include "enums.h"
#include <iostream>
using namespace std;

class Cell
{
    bool is_alive;
    uint8_t age;
    
   bound_apex_normal boa;

    public:
    void set_boa(bound_apex_normal passed_boa);
   
};